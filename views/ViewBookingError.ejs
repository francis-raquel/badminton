<!DOCTYPE html>
<html>
<head>
  <title>Slot Already Booked</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body style="font-family: Arial, sans-serif; background-color: #f8f9fa;">

  <div class="container text-center mt-5">
    <h2 class="text-danger">❌ Slot Already Booked!</h2>
    <p class="lead">The slot for <strong><%= court %></strong> on <strong><%= date %></strong> at <strong><%= time %></strong> has already been taken.</p>

    <div class="mt-4">
      <a href="/memberplayerOnly" class="btn btn-primary me-2">🏠 Home</a>
      <a href="/guestplayerOnly" class="btn btn-secondary">🔙 Back to Guest Player Page</a>
    </div>
  </div>

</body>
</html>
